import React from "react"
import { webColors } from "../../../assets/constant"

const Showing:React.FC = () => {
  return (<>
    <p style={{color : webColors['neutral-grey-500']}}>Showing 1 to 10 of 800 records</p>
    </>)
}

export default Showing